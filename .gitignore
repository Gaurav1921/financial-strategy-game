# Python
__pycache__/
*.pyc
.venv/
*.db

# Node / frontend
node_modules/
frontend/dist/

# Internal/local-only tooling - not tracked in this repo.
# VS Code's auto-init task (.vscode/tasks.json) still points at
# scripts/setup-env.ps1, which will only work on machines where that file
# exists locally - that's intentional, see docs/ENVIRONMENT_SETUP.md.
scripts/

# Editor / OS
# (.vscode/ is intentionally tracked - settings.json + tasks.json auto-init
# the conda environment on folder open, see docs/ENVIRONMENT_SETUP.md)
.DS_Store
