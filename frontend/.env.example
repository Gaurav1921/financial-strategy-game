# Copy this file to .env and adjust if your backend isn't running on the
# default local port. .env is gitignored; this example file is the
# checked-in reference for what it needs.

VITE_API_BASE=http://localhost:8000
